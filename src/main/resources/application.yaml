spring:
  application:
    name: image-processing-service

  datasource:
    url: jdbc:mysql://localhost:3306/image_processing_db
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

server:
  port: 8081
